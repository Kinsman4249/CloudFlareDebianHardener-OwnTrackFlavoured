# cf-owntracks: WebSocket upgrade-header map.
# Lives in conf.d/ so it's loaded into the `http` context once, where `map` belongs.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
